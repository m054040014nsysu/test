<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Redirecting...</title>
  <script>
    // 產生 0~1 的隨機數字
    var rand = Math.random();
    
    // 根據亂數導向問卷A或問卷B
    if (rand < 0.5) {
      window.location.href = "https://yourdomain.com/survey_A.html";
    } else {
      window.location.href = "https://yourdomain.com/survey_B.html";
    }
  </script>
</head>
<body>
  <p>Redirecting to the survey...</p>
</body>
</html>
